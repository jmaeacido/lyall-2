export async function POST(request:Request){try{const {env}=await import("cloudflare:workers");const data=await request.formData();const id=crypto.randomUUID();const photos=data.getAll("photos").filter((x):x is File=>x instanceof File&&x.size>0);if(photos.length>6)return Response.json({error:"Too many photos"},{status:400});const saved:string[]=[];for(const [i,file] of photos.entries()){if(!file.type.startsWith("image/")||file.size>10_000_000)continue;const key=`quote-requests/${id}/photo-${i+1}-${file.name.replace(/[^a-zA-Z0-9._-]/g,"_")}`;await env.BUCKET.put(key,file.stream(),{httpMetadata:{contentType:file.type}});saved.push(key)}const details={id,createdAt:new Date().toISOString(),name:String(data.get("name")||""),phone:String(data.get("phone")||""),email:String(data.get("email")||""),location:String(data.get("location")||""),service:String(data.get("service")||""),message:String(data.get("message")||""),photos:saved};await env.BUCKET.put(`quote-requests/${id}/request.json`,JSON.stringify(details,null,2),{httpMetadata:{contentType:"application/json"}});return Response.json({ok:true,id})}catch{return Response.json({error:"Unable to save request"},{status:500})}}
